@model ConfirpProject1.T_FUNCIONARIO

@{
    ViewBag.Title = "Details";
}

<h2>Detalhes do funcionário</h2>

<div>
    <h4>T_FUNCIONARIO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nm_funcionario)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nm_funcionario)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.dt_nascimento)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.dt_nascimento)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.id_funcionario }) |
    @Html.ActionLink("Retornar à lista", "Index")
</p>
